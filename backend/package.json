{
  "name": "skinwatch-backend",
  "version": "1.0.0",
  "description": "Backend for SkinWatch: holds the Google API key server-side, calls Weather + Air Quality APIs, runs the rules engine",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
