services:
  - type: web
    name: skinwatch-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: WEATHER_API_KEY
        sync: false
